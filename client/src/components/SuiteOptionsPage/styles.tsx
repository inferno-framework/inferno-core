import { makeStyles } from 'tss-react/mui';

export default makeStyles()(() => ({
  optionsList: {
    display: 'flex',
    flexDirection: 'column',
    padding: '16px 0',
    borderRadius: '16px',
    overflow: 'auto',
  },
}));
