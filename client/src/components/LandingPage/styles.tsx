import { makeStyles } from 'tss-react/mui';

export default makeStyles()(() => ({
  main: {
    display: 'flex',
    flexWrap: 'wrap',
    alignItems: 'initial',
    justifyContent: 'space-evenly',
    height: '100%',
    padding: '0 !important',
  },
}));
