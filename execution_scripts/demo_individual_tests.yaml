session:
  suite_id: demo

rules:
  - status: created
    command: "bundle exec inferno session start_run '{session_id}' -r 5.01"

  - status: done
    last_test: demo-wait_group-Test01
    command: "bundle exec inferno session start_run '{session_id}' -r 5.03"

  - status: done
    last_test: demo-wait_group-Test03
    command: "END_SCRIPT"
