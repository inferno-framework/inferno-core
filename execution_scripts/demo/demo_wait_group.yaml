sessions:
  - suite_id: demo

steps:
  - status: created
    start_run: 
      runnable: 5
  
  - status: waiting
    last_test: 5.02
    command: "curl -o /dev/null -s '{wait_outputs.wait_test_url}'"
  
  - status: done
    last_test: 5.03
    command: "END_SCRIPT"

