sessions:
  - suite: demo

steps:
  - status: created
    start_run: 
      runnable: 5
  
  - status: waiting
    last_completed: 5.02
    command: "curl -o /dev/null -s '{wait_outputs.wait_test_url}'"
  
  - status: done
    last_completed: 5
    action: END_SCRIPT

