sessions:
  - suite_id: auth_info
    preset_id: dev_auth_info_preset
    # NOTE: preset use introduces issue time skew that causes faiures - these are expected

steps:
  - status: created
    start_run:

  - status: done
    last_test: auth_info-auth_info_demo-Group02-backend_services_auth
    command: "END_SCRIPT"
