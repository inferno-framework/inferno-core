sessions:
  - suite_id: auth_info

rules:
  - status: created
    command: "bundle exec inferno session start_run '{session_id}'"

  - status: done
    last_test: auth_info-auth_info_demo-Group02-backend_services_auth
    command: "END_SCRIPT"
