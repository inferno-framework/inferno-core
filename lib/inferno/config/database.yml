development:
  adapter: sqlite
  database: ':memory:'
  max_connections: 10
  # user:
  # password:
  # host:
  # port:

production:
  adapter: sqlite
  database: ':memory:'
  max_connections: 10

test:
  adapter: sqlite
  database: ':memory:'
  max_connections: 10
